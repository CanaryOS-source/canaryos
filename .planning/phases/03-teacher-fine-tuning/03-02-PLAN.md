---
phase: 03-teacher-fine-tuning
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: false
requirements:
  - TEXT-04

must_haves:
  truths:
    - "User has run the notebook in Colab and training completed"
    - "Teacher binary F1 > 0.80 on real-world holdout (hard gate for Phase 4)"
    - "Teacher F1 > 0.95 on synthetic test set (internal quality bar)"
    - "Soft labels at T={2,3,4,5} are generated and saved"
    - "ECE calibration was measured before and after temperature scaling"
  artifacts: []
  key_links: []
---

<objective>
Verify that the user has successfully run the teacher fine-tuning notebook in Colab and that the Phase 3 gate criteria are met. This is a handoff checkpoint -- Claude created the notebook in Plan 01, the user runs it in Colab, and this checkpoint confirms the results before proceeding to Phase 4 distillation.

Purpose: Gate Phase 4. Teacher must pass F1 > 0.80 on holdout before distillation begins. This prevents compounding errors by distilling from a weak teacher.
Output: Confirmation that Phase 3 gate is passed and soft labels are ready for Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-teacher-fine-tuning/03-CONTEXT.md
@.planning/phases/03-teacher-fine-tuning/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify teacher training results meet Phase 3 gate</name>
  <files>N/A -- checkpoint only, no files modified</files>

  <read_first>
    - .planning/phases/03-teacher-fine-tuning/03-01-SUMMARY.md (understand what was built)
    - .planning/phases/03-teacher-fine-tuning/03-CONTEXT.md (D-06 holdout gate, D-12 retry configs)
  </read_first>

  <action>
This is a human-verify checkpoint. No autonomous action is taken. The user runs the notebook in Colab and reports results.

What was built: Plan 03-01 created the teacher fine-tuning notebook (`research/notebooks/teacher_finetuning.ipynb`). The user needs to:
1. Upload the notebook to Google Colab
2. Upload data files (`synthetic_scam_v1.jsonl`, `test_split.jsonl`, `holdout_realworld.jsonl`) to Colab
3. Run all cells through Cell 15
4. Report back with the gate check results from Cell 15

How to verify -- share the output of Cell 15 (Summary / Gate Check Report). Specifically confirm:
1. Synthetic test F1: Is it > 0.95? (Report exact value)
2. Holdout binary F1: Is it > 0.80? (Report exact value -- this is the HARD GATE)
3. ECE before/after calibration: What are the values?
4. Soft labels: Were all 4 files generated? (teacher_soft_labels_T2.pt, _T3.pt, _T4.pt, _T5.pt)
5. Per-vector breakdown: Which vectors (if any) had F1 below 0.60?
6. Any errors or issues during training?

If holdout F1 < 0.80:
- The notebook includes retry configs (RETRY_1, RETRY_2 per D-12)
- Change ACTIVE_CONFIG = RETRY_1 in Cell 2 and re-run training
- If both retries fail, escalate to data augmentation per D-13

If user had to use a retry config or switch to A100, note which configuration succeeded.

Resume signal: Share the Cell 15 output. Type "gate passed" if holdout F1 > 0.80, or describe the failure mode if not.
  </action>

  <verify>User confirms holdout binary F1 > 0.80 and shares Cell 15 output showing gate PASSED.</verify>

  <acceptance_criteria>
    - User reports holdout binary F1 > 0.80 (exact value provided)
    - User reports synthetic test F1 > 0.95 (exact value provided)
    - User confirms 4 soft label .pt files generated at T={2,3,4,5}
    - User confirms ECE measured before and after temperature scaling (both values reported)
    - User reports which training config succeeded (T4_CONFIG, RETRY_1, RETRY_2, or A100_CONFIG)
  </acceptance_criteria>

  <done>Phase 3 gate PASSED: teacher holdout F1 > 0.80 confirmed, soft labels at 4 temperatures ready for Phase 4 distillation, ECE calibration complete.</done>
</task>

</tasks>

<verification>
- User confirms holdout binary F1 > 0.80 (hard gate)
- User confirms synthetic test F1 > 0.95 (internal bar)
- User confirms 4 soft label .pt files exist at T={2,3,4,5}
- User confirms ECE was measured before and after calibration
</verification>

<success_criteria>
- Phase 3 gate PASSED: holdout F1 > 0.80
- Soft labels ready for Phase 4 distillation
- Teacher checkpoint saved to Google Drive
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-fine-tuning/03-02-SUMMARY.md`
</output>
