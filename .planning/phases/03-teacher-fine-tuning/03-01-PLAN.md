---
phase: 03-teacher-fine-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - research/notebooks/teacher_finetuning.ipynb
autonomous: true
requirements:
  - TEXT-04

user_setup:
  - service: google-colab
    why: "DeBERTa-v3-large (435M params) requires GPU for fine-tuning; Colab provides free T4 (16GB VRAM)"
    env_vars: []
    dashboard_config:
      - task: "Mount Google Drive for checkpoint persistence"
        location: "Notebook Cell 1 runs drive.mount() -- user approves access popup"
      - task: "Upload training data to Colab"
        location: "Copy research/data/synthetic_scam_v1.jsonl, test_split.jsonl, holdout_realworld.jsonl to Colab working directory"

must_haves:
  truths:
    - "Notebook defines a dual-head DeBERTa-v3-large model with binary scam/safe head and 8 sigmoid intent heads sharing one encoder"
    - "Training is configured for T4 GPU: batch size 4, gradient accumulation 4, FP16, gradient checkpointing, max_length 128"
    - "Every epoch checkpoint is saved to Google Drive at /content/drive/MyDrive/canaryos_teacher/"
    - "Training can resume from any checkpoint after Colab session timeout"
    - "Holdout evaluation cell asserts F1 > 0.80 and prints per-vector error breakdown"
    - "Synthetic test evaluation cell asserts F1 > 0.95"
    - "ECE is measured before and after temperature scaling on a calibration set carved from val split"
    - "Soft labels at T={2,3,4,5} are pre-computed and saved to research/data/teacher_soft_labels_T{N}.pt with both binary and intent logits"
    - "A T4-to-A100 migration section explains how to resume training on Colab Pro"
  artifacts:
    - path: "research/notebooks/teacher_finetuning.ipynb"
      provides: "Complete teacher fine-tuning notebook for Colab execution"
      min_lines: 400
  key_links:
    - from: "research/notebooks/teacher_finetuning.ipynb"
      to: "research/data/synthetic_scam_v1.jsonl"
      via: "Data loading cell reads JSONL with split field"
      pattern: "synthetic_scam_v1.jsonl"
    - from: "research/notebooks/teacher_finetuning.ipynb"
      to: "research/data/holdout_realworld.jsonl"
      via: "Holdout evaluation cell loads and evaluates"
      pattern: "holdout_realworld.jsonl"
    - from: "research/notebooks/teacher_finetuning.ipynb"
      to: "/content/drive/MyDrive/canaryos_teacher/"
      via: "Checkpoint save/resume functions"
      pattern: "canaryos_teacher"
    - from: "research/notebooks/teacher_finetuning.ipynb"
      to: "research/data/teacher_soft_labels_T{N}.pt"
      via: "Soft label pre-computation cell saves .pt files"
      pattern: "teacher_soft_labels_T"
---

<objective>
Create the complete teacher fine-tuning Jupyter notebook for Phase 3. This notebook fine-tunes DeBERTa-v3-large (435M params) on the Phase 1 synthetic dataset with a dual-head architecture (binary scam/safe + 8 sigmoid intent heads), evaluates against both synthetic test and real-world holdout, measures ECE calibration, pre-computes soft labels at four temperatures for Phase 4 distillation, and includes a T4-to-A100 migration guide.

Purpose: Establish the teacher accuracy ceiling and produce calibrated soft labels that Phase 4 will use to distill knowledge into MobileBERT. The teacher never deploys to device.
Output: `research/notebooks/teacher_finetuning.ipynb` -- ready for user to upload to Colab and run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-teacher-fine-tuning/03-CONTEXT.md
@.planning/phases/03-teacher-fine-tuning/03-RESEARCH.md
@research/notebooks/architecture_benchmark.ipynb (reuse data loading patterns from Cell 2)
@research/data/synthetic_scam_v1.jsonl (first line -- check field names: text, label, vector, channel, source, split)
@research/data/holdout_realworld.jsonl (first line -- check field names: text, label, vector, source)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create teacher notebook -- training pipeline (Cells 1-9)</name>
  <files>research/notebooks/teacher_finetuning.ipynb</files>

  <read_first>
    - research/notebooks/architecture_benchmark.ipynb (Cell 2 data loading pattern, Cell 3 training config pattern)
    - research/data/synthetic_scam_v1.jsonl (first 3 lines -- verify field names: text, label, vector, split)
    - research/data/holdout_realworld.jsonl (first 3 lines -- verify field names: text, label, vector, source -- NOTE: no "split" field)
    - .planning/phases/03-teacher-fine-tuning/03-RESEARCH.md (Pattern 1-3 for model class, label mapping, checkpoint resume; hyperparameter configs; cell structure recommendation)
    - .planning/phases/03-teacher-fine-tuning/03-CONTEXT.md (all D-01 through D-14 decisions)
  </read_first>

  <action>
Create `research/notebooks/teacher_finetuning.ipynb` as a Jupyter notebook with the following cells. Use the nbformat library to create the notebook programmatically (as done in Phase 2).

**Cell 1 (code): Environment Setup + Drive Mount (per D-01, D-02)**
```python
!pip install -q transformers>=4.48.0 datasets>=3.0.0 accelerate>=0.25.0 evaluate>=0.4.0 torchmetrics>=1.2.0 sentencepiece
import torch
print(f"PyTorch: {torch.__version__}")
print(f"CUDA available: {torch.cuda.is_available()}")
if torch.cuda.is_available():
    print(f"GPU: {torch.cuda.get_device_name(0)}")
    print(f"VRAM: {torch.cuda.get_device_properties(0).total_mem / 1024**3:.1f} GB")
from google.colab import drive
drive.mount("/content/drive")
import os
CHECKPOINT_DIR = "/content/drive/MyDrive/canaryos_teacher"
os.makedirs(CHECKPOINT_DIR, exist_ok=True)
print(f"Checkpoint directory: {CHECKPOINT_DIR}")
```

**Cell 2 (code): Configuration (per D-02, D-03)**
Define two config dicts -- `T4_CONFIG` and `A100_CONFIG` -- plus the active config selector. T4 config: `per_device_train_batch_size=4`, `gradient_accumulation_steps=4`, `learning_rate=8e-6`, `num_train_epochs=3`, `warmup_ratio=0.1`, `weight_decay=0.01`, `lr_scheduler_type="cosine"`, `fp16=True`, `gradient_checkpointing=True`, `max_grad_norm=1.0`, `max_seq_length=128`. A100 config: same except `batch_size=16`, `grad_accum=1`, `gradient_checkpointing=False`. Also define retry configs per D-12: `RETRY_1` (lr=3e-6, epochs=5, warmup=0.15) and `RETRY_2` (lr=6e-6, epochs=4, with class weighting flag). Include `ACTIVE_CONFIG = T4_CONFIG` selector with comment explaining how to switch. Define paths: `DATA_DIR`, `MODEL_NAME = "microsoft/deberta-v3-large"`, `DRIVE_CHECKPOINT_DIR`.

**Cell 3 (code): Data Loading + Intent Label Mapping (per D-05)**
Define `INTENT_LABELS` list: `["urgency", "authority", "financial_request", "remote_access", "reward_lottery", "impersonation", "romance_grooming", "crypto"]`.
Define `VECTOR_TO_INTENTS` dict mapping each of the 8 scam vectors + "safe" to 8-dim binary lists (use the mapping from RESEARCH.md Pattern 2).
Create a `ScamDataset(torch.utils.data.Dataset)` class that:
- Loads JSONL, filters by `split` field if provided
- Tokenizes with `AutoTokenizer.from_pretrained("microsoft/deberta-v3-large")`, `max_length=128`, `padding="max_length"`, `truncation=True`
- Returns dict with `input_ids`, `attention_mask`, `labels` (0=safe, 1=scam as `torch.long`), `intent_labels` (8-dim float tensor from VECTOR_TO_INTENTS)
Load tokenizer globally. Load train, val, and test datasets. Print counts per split and per-vector distribution for train set. Also load holdout separately (holdout JSONL has no `split` field -- load all rows).

**Cell 4 (code): Model Definition (per D-05)**
Define `DualHeadDeBERTaTeacher(DebertaV2PreTrainedModel)`:
- `__init__`: `DebertaV2Model(config)`, `nn.Dropout(config.hidden_dropout_prob)`, `nn.Linear(config.hidden_size, 2)` for binary_head, `nn.Linear(config.hidden_size, 8)` for intent_head, call `self.post_init()`
- `forward(input_ids, attention_mask, token_type_ids=None, labels=None, intent_labels=None)`:
  - Get `outputs = self.deberta(input_ids, attention_mask, token_type_ids)`
  - `cls_output = self.dropout(outputs.last_hidden_state[:, 0, :])`
  - `binary_logits = self.binary_head(cls_output)` shape [batch, 2]
  - `intent_logits = self.intent_head(cls_output)` shape [batch, 8]
  - If `labels is not None`: compute `binary_loss = CrossEntropyLoss()(binary_logits, labels)`, if `intent_labels is not None` also compute `intent_loss = BCEWithLogitsLoss()(intent_logits, intent_labels.float())`, combined `loss = 0.7 * binary_loss + 0.3 * intent_loss` (else just binary_loss)
  - Return dict `{"loss": loss, "binary_logits": binary_logits, "intent_logits": intent_logits}`
Load model: `DualHeadDeBERTaTeacher.from_pretrained(MODEL_NAME)`. Enable gradient checkpointing. Print param count.

**Cell 5 (code): Custom Trainer with Dual-Head Loss**
Subclass `transformers.Trainer` as `DualHeadTrainer`:
- Override `compute_loss(model, inputs, return_outputs=False)`:
  - Extract `labels` and `intent_labels` from inputs
  - Forward pass: `outputs = model(input_ids=inputs["input_ids"], attention_mask=inputs["attention_mask"], labels=labels, intent_labels=intent_labels)`
  - Return `(outputs["loss"], outputs) if return_outputs else outputs["loss"]`
- Define custom `data_collator` function that properly collates the `intent_labels` field alongside standard fields.
- Define `compute_metrics` function that computes binary F1, precision, recall from predictions using `evaluate.load("f1")`, `evaluate.load("precision")`, `evaluate.load("recall")`. The predictions come from `binary_logits` (argmax over dim=-1).

**Cell 6 (code): Checkpoint Resume Logic (per D-02, D-04)**
Define `save_checkpoint(trainer, epoch, metrics, drive_dir)` -- saves model, optimizer, scheduler, training_state.json with epoch number, metrics, timestamp.
Define `load_latest_checkpoint(drive_dir)` -- reads training_state.json, returns path to latest completed epoch checkpoint dir (or None if no checkpoint exists).
Print resume status: "Resuming from epoch X" or "Starting fresh training".

**Cell 7 (code): Training Execution**
Set up `TrainingArguments` from `ACTIVE_CONFIG`:
- `output_dir=DRIVE_CHECKPOINT_DIR`
- `evaluation_strategy="epoch"`, `save_strategy="epoch"`, `save_total_limit=None` (keep ALL per D-04)
- `load_best_model_at_end=True`, `metric_for_best_model="f1"`
- `fp16=ACTIVE_CONFIG["fp16"]`
- `per_device_train_batch_size`, `gradient_accumulation_steps`, `learning_rate`, `num_train_epochs`, `warmup_ratio`, `weight_decay`, `lr_scheduler_type`, `max_grad_norm` from config
- `logging_steps=50`, `report_to="none"`
Create `DualHeadTrainer(model, args, train_dataset, eval_dataset=val_dataset, data_collator, compute_metrics)`.
Check for existing checkpoint via `load_latest_checkpoint()`. If found, call `trainer.train(resume_from_checkpoint=checkpoint_path)`. Else `trainer.train()`.
After training, save best model to `DRIVE_CHECKPOINT_DIR/best_binary/`.
Print training complete message with total time.

**Cell 8 (code): Synthetic Test Evaluation (per D-07, TEXT-04-T1)**
Load synthetic test split from `test_split.jsonl`.
Run `trainer.predict(test_dataset)`.
Compute binary F1, precision, recall.
Print classification report.
Assert `synthetic_f1 > 0.95` with descriptive error message.
Print: `"SYNTHETIC TEST GATE: PASSED (F1={value})"` or `"FAILED"`.

**Cell 9 (code): Holdout Evaluation + Per-Vector Breakdown (per D-06, D-14, TEXT-04-T2)**
Load holdout from `holdout_realworld.jsonl` (no split field -- load all).
Run inference on holdout.
Compute overall binary F1, precision, recall.
Assert `holdout_f1 > 0.80` with descriptive error message including the actual F1 value.
Print classification report (binary).
Compute per-vector breakdown: for each vector in holdout, compute F1/precision/recall and sample count. Display as a formatted table using pandas or tabulate. Sort by F1 ascending (worst vectors first). Add a note for vectors with <10 samples that per-vector metrics are unreliable.
Also compute and print intent head metrics: per-label precision/recall on holdout samples where intent ground truth is available (from VECTOR_TO_INTENTS mapping). Note these are informational only (per D-06).
Print: `"HOLDOUT GATE: PASSED (F1={value})"` or `"FAILED -- see per-vector breakdown above"`.

The notebook file should be created using `nbformat` (v4). Use `nbformat.v4.new_notebook()`, `new_code_cell()`, and `new_markdown_cell()` as appropriate. Each code cell should have clear comments at the top identifying its purpose and which decisions (D-xx) it implements.
  </action>

  <verify>
    <automated>cd /Users/saiamartya/Desktop/ClaudeWorkspace/CanaryOS/canaryos && python3 -c "
import json
with open('research/notebooks/teacher_finetuning.ipynb') as f:
    nb = json.load(f)
cells = nb['cells']
src_all = ' '.join(''.join(c['source']) for c in cells)
checks = [
    ('cell_count', len([c for c in cells if c['cell_type']=='code']) >= 9),
    ('deberta_v3_large', 'deberta-v3-large' in src_all),
    ('dual_head_class', 'DualHeadDeBERTaTeacher' in src_all),
    ('binary_head', 'binary_head' in src_all),
    ('intent_head', 'intent_head' in src_all),
    ('intent_labels', 'INTENT_LABELS' in src_all),
    ('vector_to_intents', 'VECTOR_TO_INTENTS' in src_all),
    ('drive_mount', 'drive.mount' in src_all),
    ('checkpoint_dir', 'canaryos_teacher' in src_all),
    ('sentencepiece', 'sentencepiece' in src_all),
    ('gradient_checkpointing', 'gradient_checkpointing' in src_all),
    ('fp16', 'fp16' in src_all),
    ('batch_size_4', 'per_device_train_batch_size' in src_all),
    ('grad_accum_4', 'gradient_accumulation_steps' in src_all),
    ('lr_8e6', '8e-6' in src_all),
    ('cosine', 'cosine' in src_all),
    ('holdout_gate', '0.80' in src_all or '0.8' in src_all),
    ('synthetic_gate', '0.95' in src_all),
    ('resume_checkpoint', 'resume_from_checkpoint' in src_all),
    ('synthetic_scam_v1', 'synthetic_scam_v1.jsonl' in src_all),
    ('holdout_realworld', 'holdout_realworld.jsonl' in src_all),
    ('retry_config', 'RETRY_1' in src_all or 'retry_1' in src_all.lower()),
    ('a100_config', 'A100_CONFIG' in src_all),
    ('per_vector', 'per_vector' in src_all.lower() or 'per-vector' in src_all.lower() or 'by vector' in src_all.lower()),
]
passed = sum(1 for _, v in checks if v)
total = len(checks)
for name, val in checks:
    print(f'  {\"PASS\" if val else \"FAIL\"}: {name}')
print(f'Result: {passed}/{total} checks passed')
assert passed == total, f'FAILED: {total - passed} checks did not pass'
"
    </automated>
  </verify>

  <acceptance_criteria>
    - research/notebooks/teacher_finetuning.ipynb exists and is valid JSON (nbformat v4)
    - Notebook has >= 9 code cells
    - Cell 1 contains `!pip install` with `sentencepiece`, `drive.mount("/content/drive")`
    - Cell 2 contains `T4_CONFIG` with `per_device_train_batch_size: 4`, `gradient_accumulation_steps: 4`, `learning_rate: 8e-6`, and `A100_CONFIG` with `per_device_train_batch_size: 16`
    - Cell 2 contains `RETRY_1` and `RETRY_2` config dicts per D-12
    - Cell 3 contains `INTENT_LABELS` list of 8 strings, `VECTOR_TO_INTENTS` dict mapping 9 vectors
    - Cell 3 loads `synthetic_scam_v1.jsonl` with split filtering and `holdout_realworld.jsonl`
    - Cell 4 defines `DualHeadDeBERTaTeacher(DebertaV2PreTrainedModel)` with `binary_head = nn.Linear(config.hidden_size, 2)` and `intent_head = nn.Linear(config.hidden_size, 8)`
    - Cell 4 `forward()` computes combined loss `0.7 * binary_loss + 0.3 * intent_loss`
    - Cell 5 defines `DualHeadTrainer(Trainer)` with `compute_loss` override
    - Cell 6 defines `save_checkpoint()` and `load_latest_checkpoint()` targeting `/content/drive/MyDrive/canaryos_teacher/`
    - Cell 7 creates TrainingArguments with `save_total_limit=None`, `load_best_model_at_end=True`, `metric_for_best_model="f1"`, calls `trainer.train(resume_from_checkpoint=...)` if checkpoint exists
    - Cell 8 evaluates on test_split.jsonl and asserts `synthetic_f1 > 0.95`
    - Cell 9 evaluates on holdout_realworld.jsonl, asserts `holdout_f1 > 0.80`, prints per-vector F1 breakdown sorted by F1 ascending
  </acceptance_criteria>

  <done>Notebook cells 1-9 exist implementing the complete training pipeline: Colab setup with sentencepiece and drive mount, T4/A100/retry configs, data loading with intent label mapping, DualHeadDeBERTaTeacher model class, DualHeadTrainer with custom loss, checkpoint resume from Google Drive, training execution, synthetic test gate (F1>0.95), and holdout gate (F1>0.80) with per-vector error breakdown.</done>
</task>

<task type="auto">
  <name>Task 2: Complete notebook -- calibration, soft labels, migration guide (Cells 10-15)</name>
  <files>research/notebooks/teacher_finetuning.ipynb</files>

  <read_first>
    - research/notebooks/teacher_finetuning.ipynb (the notebook created in Task 1 -- read to understand cell numbering and variable names)
    - .planning/phases/03-teacher-fine-tuning/03-RESEARCH.md (Pattern 4: soft label pre-computation, Pattern 5: ECE calibration, T4-to-A100 migration checklist, retry configs)
    - .planning/phases/03-teacher-fine-tuning/03-CONTEXT.md (D-03 migration guide requirements, D-08/D-09/D-10/D-11 soft label and calibration decisions)
  </read_first>

  <action>
Open the notebook created in Task 1 and append cells 10-15. Read the existing notebook first to get the correct variable names (model, trainer, tokenizer, val_dataset, DRIVE_CHECKPOINT_DIR, etc.).

**Cell 10 (code): ECE Measurement Before Calibration (per D-11, TEXT-04-T4)**
```python
from torchmetrics.classification import BinaryCalibrationError
import torch.nn.functional as F

def measure_ece(model, dataloader, device, n_bins=15):
    ece_metric = BinaryCalibrationError(n_bins=n_bins, norm="l1")
    model.eval()
    all_probs, all_labels = [], []
    with torch.no_grad():
        for batch in dataloader:
            input_ids = batch["input_ids"].to(device)
            attention_mask = batch["attention_mask"].to(device)
            labels = batch["labels"]
            outputs = model(input_ids=input_ids, attention_mask=attention_mask)
            probs = F.softmax(outputs["binary_logits"], dim=-1)[:, 1]
            all_probs.append(probs.cpu())
            all_labels.append(labels)
            ece_metric.update(probs.cpu(), labels)
    ece = ece_metric.compute().item()
    return ece, torch.cat(all_probs), torch.cat(all_labels)
```
Use 20% of val_dataset as calibration set (first 20% by index, ~459 samples). Create a DataLoader from the calibration subset. Compute ECE before calibration. Print `"ECE (before calibration): {ece:.4f}"`. Plot a reliability diagram (10 bins, predicted probability vs actual frequency) using matplotlib.

**Cell 11 (code): Temperature Scaling + ECE After Calibration (per D-11)**
Define `find_optimal_temperature(model, cal_dataloader, device)` that searches temperatures from 0.5 to 5.0 in 0.1 increments, computing ECE at each, returning the temperature that minimizes ECE.
Run on calibration set. Print `"Optimal calibration temperature: {best_t:.1f}"`. Compute ECE after applying optimal temperature. Print `"ECE (after calibration): {ece_after:.4f}"`. Print improvement: `"ECE reduction: {ece_before - ece_after:.4f}"`.
Plot reliability diagram after calibration side-by-side with before.

**Cell 12 (code): Soft Label Pre-Computation (per D-08, D-09, D-10, TEXT-04-T5)**
Define `precompute_soft_labels(model, dataloader, temperatures, device, save_dir)`:
- For each T in `[2, 3, 4, 5]`:
  - Run full training set through model (inference only, torch.no_grad)
  - Binary soft labels: `F.softmax(binary_logits / T, dim=-1)` shape [N, 2]
  - Intent soft labels: `torch.sigmoid(intent_logits / T)` shape [N, 8]
  - Save as `{"binary_soft_labels": tensor, "intent_soft_labels": tensor, "temperature": T}` to `{save_dir}/teacher_soft_labels_T{T}.pt`
  - Print shape and file size for each
Create DataLoader from full training set (batch_size=32, no shuffle). Run precompute. Verify all 4 files exist. Print summary table of file sizes.
Note: save_dir should be the local `research/data/` path. Also save copies to Google Drive for persistence.

**Cell 13 (code): Save Final Checkpoint (per TEXT-04-T3)**
Save best model to `research/models/teacher_finetuned/` (local) using `model.save_pretrained()` and `tokenizer.save_pretrained()`.
Also save to `DRIVE_CHECKPOINT_DIR/best_binary/`.
Verify checkpoint exists: `assert os.path.exists("research/models/teacher_finetuned/config.json")`.
Print model file sizes.

**Cell 14 (markdown + code): T4-to-A100 Migration Guide (per D-03)**
Markdown cell with heading "## T4-to-A100 Migration Guide" explaining:
1. When to migrate (OOM on T4, want faster training, session timeout frustration)
2. What changes: table showing T4 vs A100 settings (batch_size 4->16, grad_accum 4->1, gradient_checkpointing True->False)
3. Step-by-step instructions:
   a. Open new Colab notebook, select A100 runtime
   b. Run Cell 1 (installs + drive mount)
   c. Run Cell 2, change `ACTIVE_CONFIG = A100_CONFIG`
   d. Run Cells 3-4 (data + model -- these are config-independent)
   e. Run Cell 6 -- it auto-detects latest checkpoint from Drive
   f. Run Cell 7 -- Trainer resumes from checkpoint with new batch settings
4. What to expect: ~3-4x faster training, same effective batch size (16), identical model quality
5. Checkpoint compatibility note: checkpoints are optimizer-state compatible across batch sizes since effective batch size is unchanged

Code cell after markdown: a helper that prints current checkpoint status from Drive (epochs completed, best metrics so far, estimated remaining time).

**Cell 15 (code): Summary / Gate Check Report**
Print a formatted summary:
- Training config used (T4 or A100)
- Synthetic test F1 (PASS/FAIL vs 0.95)
- Holdout F1 (PASS/FAIL vs 0.80)
- ECE before/after calibration
- Soft labels generated: list all 4 .pt files with sizes
- Per-vector holdout breakdown (from Cell 9 results)
- Overall gate status: "PHASE 3 GATE: PASSED -- ready for Phase 4 distillation" or "PHASE 3 GATE: FAILED -- see above for issues"
- If failed: print which retry config to try next (RETRY_1 or RETRY_2 per D-12), or escalate to data augmentation per D-13 if both retries exhausted.

Append all new cells to the notebook using nbformat, preserving existing cells. Write the updated notebook.
  </action>

  <verify>
    <automated>cd /Users/saiamartya/Desktop/ClaudeWorkspace/CanaryOS/canaryos && python3 -c "
import json
with open('research/notebooks/teacher_finetuning.ipynb') as f:
    nb = json.load(f)
cells = nb['cells']
code_cells = [c for c in cells if c['cell_type']=='code']
src_all = ' '.join(''.join(c['source']) for c in cells)
checks = [
    ('total_code_cells_ge_14', len(code_cells) >= 14),
    ('ece_metric', 'BinaryCalibrationError' in src_all),
    ('measure_ece', 'measure_ece' in src_all),
    ('reliability_diagram', 'reliability' in src_all.lower() or 'calibration' in src_all.lower()),
    ('find_optimal_temperature', 'find_optimal_temperature' in src_all or 'optimal_temperature' in src_all or 'best_t' in src_all),
    ('soft_labels_T2', 'teacher_soft_labels_T2' in src_all),
    ('soft_labels_T4', 'teacher_soft_labels_T4' in src_all),
    ('soft_labels_T5', 'teacher_soft_labels_T5' in src_all),
    ('binary_soft_labels', 'binary_soft_labels' in src_all),
    ('intent_soft_labels', 'intent_soft_labels' in src_all),
    ('save_pretrained', 'save_pretrained' in src_all),
    ('teacher_finetuned_dir', 'teacher_finetuned' in src_all),
    ('migration_guide', 'migration' in src_all.lower()),
    ('a100_in_migration', 'A100' in src_all),
    ('gate_check', 'PHASE 3 GATE' in src_all or 'Phase 3' in src_all),
    ('retry_guidance', 'RETRY_1' in src_all or 'retry' in src_all.lower()),
]
passed = sum(1 for _, v in checks if v)
total = len(checks)
for name, val in checks:
    print(f'  {\"PASS\" if val else \"FAIL\"}: {name}')
print(f'Result: {passed}/{total} checks passed')
assert passed == total, f'FAILED: {total - passed} checks did not pass'
"
    </automated>
  </verify>

  <acceptance_criteria>
    - Notebook now has >= 14 code cells (9 from Task 1 + 5-6 new) plus markdown cells
    - Cell 10 contains `BinaryCalibrationError` import and `measure_ece()` function, uses 20% of val as calibration set
    - Cell 11 contains `find_optimal_temperature()` searching 0.5-5.0 range, prints ECE before and after
    - Cell 12 contains `precompute_soft_labels()` saving 4 .pt files (`teacher_soft_labels_T2.pt` through `teacher_soft_labels_T5.pt`), each containing both `binary_soft_labels` and `intent_soft_labels` tensors
    - Cell 13 saves model to `research/models/teacher_finetuned/` with `save_pretrained()` and asserts config.json exists
    - Cell 14 contains T4-to-A100 migration guide with settings table and step-by-step resume instructions per D-03
    - Cell 15 prints gate check summary (synthetic F1 vs 0.95, holdout F1 vs 0.80, ECE values, soft label files, overall PASS/FAIL)
    - Cell 15 includes retry guidance referencing RETRY_1/RETRY_2 configs per D-12 and data augmentation escalation per D-13
  </acceptance_criteria>

  <done>Complete notebook with all 15 cells: training pipeline (cells 1-9) plus ECE calibration with reliability diagrams, temperature-optimal soft label pre-computation at T={2,3,4,5}, final checkpoint save, T4-to-A100 migration guide, and summary gate check with retry/escalation guidance.</done>
</task>

</tasks>

<verification>
1. `research/notebooks/teacher_finetuning.ipynb` is valid JSON and loads with nbformat
2. Notebook has >= 14 code cells covering all 15 logical sections
3. All D-01 through D-14 decisions from CONTEXT.md are addressed:
   - D-01: Colab-specific patterns (pip install, drive mount)
   - D-02: T4 config (batch 4, grad accum 4, FP16, gradient checkpointing)
   - D-03: A100 migration section with resume instructions
   - D-04: All checkpoints to Google Drive, every epoch saved
   - D-05: Dual-head model (binary + 8 intent)
   - D-06: Holdout F1 > 0.80 is only hard gate; intent logged but not blocking
   - D-07: Synthetic F1 > 0.95 internal bar
   - D-08: Soft labels pre-computed at end of Phase 3
   - D-09: Four temperatures T={2,3,4,5}
   - D-10: Both binary and intent logits in soft labels
   - D-11: ECE measured before and after calibration
   - D-12: Two retry configs defined
   - D-13: Escalation to data augmentation mentioned in summary cell
   - D-14: Per-vector error breakdown in Cell 9
4. No .py files created (all work in .ipynb per CLAUDE.md)
</verification>

<success_criteria>
- `research/notebooks/teacher_finetuning.ipynb` exists, is valid nbformat v4, has >= 14 code cells
- DualHeadDeBERTaTeacher class with binary_head (2 classes) and intent_head (8 sigmoids) on shared DebertaV2Model encoder
- T4 config: batch 4, grad accum 4, lr 8e-6, FP16, gradient checkpointing, max_length 128
- Checkpoint resume from Google Drive works (save_checkpoint + load_latest_checkpoint pattern)
- Holdout evaluation asserts F1 > 0.80 with per-vector breakdown
- ECE measured before and after temperature scaling
- Soft labels saved at T={2,3,4,5} with both binary and intent logits
- T4-to-A100 migration guide included
- Summary cell reports overall gate PASS/FAIL with retry guidance
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-fine-tuning/03-01-SUMMARY.md`
</output>
