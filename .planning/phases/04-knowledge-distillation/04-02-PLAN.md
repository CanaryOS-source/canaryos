---
phase: 04-knowledge-distillation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - research/notebooks/knowledge_distillation.ipynb
autonomous: false
requirements:
  - TEXT-04

must_haves:
  truths:
    - "Phase B intermediate layer training adds hidden state MSE + attention KL losses on top of Phase A soft-label loss"
    - "Temperature sweep evaluates T={2,3,4,5} on holdout and selects optimal T by holdout F1 (not training loss)"
    - "Gate check cell compares distilled student F1 against baseline (0.7719) and gate (0.8019), triggers D-06 recovery if gate fails"
    - "Final student checkpoint saved to research/models/student_finetuned/ for Phase 6 QAT consumption"
    - "User can run the complete notebook in Colab and verify distillation results"
  artifacts:
    - path: "research/notebooks/knowledge_distillation.ipynb"
      provides: "Complete distillation notebook: Cells 11-16 adding Phase B training, temperature sweep, gate check, checkpoint save, migration guide"
      min_lines: 800
  key_links:
    - from: "research/notebooks/knowledge_distillation.ipynb (Cell 11)"
      to: "DistillationWrapper.compute_intermediate_loss"
      via: "Phase B training loop calls compute_total_loss_phase_b with hidden_loss and attention_loss"
      pattern: "compute_total_loss_phase_b|compute_intermediate_loss"
    - from: "research/notebooks/knowledge_distillation.ipynb (Cell 12)"
      to: "Holdout evaluation"
      via: "evaluate_holdout called for each temperature T={2,3,4,5}"
      pattern: "for T in.*2.*3.*4.*5"
    - from: "research/notebooks/knowledge_distillation.ipynb (Cell 14)"
      to: "research/models/student_finetuned/"
      via: "torch.save final checkpoint"
      pattern: "student_finetuned"
---

<objective>
Complete the knowledge distillation notebook (Cells 11-16) with Phase B intermediate layer training, temperature sweep, gate check, final checkpoint save, and T4-to-A100 migration guide. Then verify the complete notebook with the user running it in Colab.

Purpose: Phase B adds the intermediate layer transfer (hidden state + attention alignment) that TEXT-04 mandates. The temperature sweep selects optimal T from {2,3,4,5} by holdout F1. The gate check determines whether the student meets the 3 F1-point improvement requirement. The final checkpoint is the input to Phase 6 QAT.

Output: Complete `research/notebooks/knowledge_distillation.ipynb` (Cells 0-16, ~1000+ lines), verified by user in Colab
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-knowledge-distillation/04-CONTEXT.md
@.planning/phases/04-knowledge-distillation/04-RESEARCH.md
@.planning/phases/04-knowledge-distillation/04-01-SUMMARY.md
@research/notebooks/knowledge_distillation.ipynb
@research/notebooks/teacher_finetuning.ipynb

<interfaces>
<!-- From Plan 01 outputs that this plan extends -->

DistillationWrapper class (Cell 7):
- self.student: MobileBertForSequenceClassification with output_hidden_states=True, output_attentions=True
- self.hidden_projections: nn.ModuleList of 24 nn.Linear(1024, 512)
- self.heads_per_group: 4 (16 teacher heads / 4 student heads)
- compute_intermediate_loss(teacher_outputs, student_outputs) -> (hidden_loss, attention_loss)

Loss functions (Cell 7):
- compute_soft_label_loss(student_logits, soft_labels, temperature) -> KL div * T^2
- compute_hard_label_loss(student_logits, hard_labels) -> CE loss
- compute_total_loss_phase_a(student_logits, soft_labels, hard_labels, T, alpha) -> alpha*soft + (1-alpha)*hard
- compute_total_loss_phase_b(student_logits, soft_labels, hard_labels, hidden_loss, attention_loss, T, alpha, beta, gamma) -> alpha*soft + (1-alpha)*hard + beta*hidden + gamma*attention

Phase A checkpoint (Cell 10):
- PATHS['STUDENT_CHECKPOINT_DIR'] + '/phase_a_final.pt'
- Contains: model state_dict, optimizer state_dict, epoch, loss

evaluate_holdout function (Cell 9):
- evaluate_holdout(model, holdout_loader, device, label) -> f1_score

Configuration (Cell 2):
- ACTIVE_CONFIG with: alpha=0.5, beta=100.0, gamma=1.0, epochs_phase_b=5, temperature=4
- BASELINE_F1 = 0.7719, GATE_F1 = 0.8019, TEACHER_F1 = 0.8052
- PATHS dict with Drive locations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase B training, temperature sweep, gate check, and checkpoint save (Cells 11-16)</name>
  <files>research/notebooks/knowledge_distillation.ipynb</files>
  <read_first>
    - research/notebooks/knowledge_distillation.ipynb (current state with Cells 0-10 from Plan 01)
    - .planning/phases/04-knowledge-distillation/04-RESEARCH.md (Phase B loss formula, temperature sweep pattern, anti-patterns, Pitfall 2 beta calibration, Pitfall 3 DeBERTa attention)
    - .planning/phases/04-knowledge-distillation/04-CONTEXT.md (D-01 both phases run, D-02 intermediate always added, D-06 recovery, D-09 checkpointing)
    - research/notebooks/teacher_finetuning.ipynb (T4-to-A100 migration guide pattern from Cell 15)
  </read_first>
  <action>
Add Cells 11-16 to the existing knowledge_distillation.ipynb notebook.

**Cell 11 -- Phase B training loop (intermediate layer transfer, per D-01/D-02):**
```python
def train_phase_b(model, teacher, soft_label_loader, optimizer, scheduler, config, device):
    """Phase B: Full distillation with intermediate layer transfer.
    Continues from Phase A checkpoint. Per D-02, ALWAYS runs regardless of Phase A results.
    Per D-03, teacher runs live forward pass each batch for intermediate representations.
    """
```

Key implementation details:
- Load Phase A final checkpoint as starting point (NOT from scratch -- Phase B builds on Phase A)
- Create new optimizer (AdamW) and scheduler for Phase B (fresh LR schedule, potentially lower LR: ACTIVE_CONFIG['learning_rate'] * 0.5 for stability when adding new losses)
- Projection layers are added to optimizer parameter groups: model.hidden_projections.parameters()
- For each epoch (ACTIVE_CONFIG['epochs_phase_b']):
  - For each batch:
    - Teacher forward pass with torch.no_grad() (D-04): teacher(input_ids, attention_mask) with output_hidden_states=True, output_attentions=True
    - Student forward pass: model(input_ids, attention_mask) with output_hidden_states=True, output_attentions=True
    - Compute intermediate losses: hidden_loss, attention_loss = model.compute_intermediate_loss(teacher_out, student_out)
    - Compute total loss: compute_total_loss_phase_b(student_logits, soft_labels, hard_labels, hidden_loss, attention_loss, T, alpha, beta, gamma)
    - **CRITICAL per RESEARCH.md Pitfall 2:** In the FIRST 10 batches of epoch 0, log individual loss components: soft_loss, hard_loss, beta*hidden_loss, gamma*attention_loss. Print a warning if beta*hidden_loss is < 0.1x or > 10x of soft_loss, suggesting beta adjustment.
    - Backward pass, gradient clipping (max_norm=1.0), optimizer step, scheduler step
  - End of epoch: print epoch, avg_total_loss, avg_hidden_loss, avg_attention_loss, learning_rate
  - Checkpoint save to Google Drive (D-09): PATHS['STUDENT_CHECKPOINT_DIR'] + '/phase_b_epoch_{N}.pt'
  - Also save phase_b_latest.pt (overwrite)

Add checkpoint resume logic: if phase_b_latest.pt exists, load and continue.

**CRITICAL per RESEARCH.md Pitfall 3 (DeBERTa attention):** Add a configurable flag `use_attention_loss = True`. If attention loss fails to converge (plateaus at high value while hidden loss decreases), the user can set this to False and re-run Phase B with hidden states + soft labels only (dropping attention alignment). Print a note: "If attention_loss does not converge, re-run with use_attention_loss=False. Hidden state alignment alone can be sufficient per MiniLM findings."

Print total Phase B training time at end.

**Cell 12 -- Temperature sweep (TEXT-04 requirement):**
```python
def temperature_sweep(model, teacher, train_dataset, holdout_loader, config, device):
    """Sweep T={2,3,4,5}. Train separate models at each T from Phase A checkpoint.
    Select optimal T by holdout F1, NOT by training loss (TEXT-04 explicit requirement).
    """
```

Per RESEARCH.md Pitfall 5 (Colab timeout): Structure sweep so each temperature trains from the SAME Phase A base checkpoint. Save each temperature's final model separately. Resume from where left off.

Implementation:
- For each T in [2, 3, 4, 5]:
  - Load Phase A checkpoint as base
  - Load soft labels at temperature T from PATHS['SOFT_LABELS_DIR']/teacher_soft_labels_T{T}.pt
  - Run Phase B training (abbreviated: ACTIVE_CONFIG['epochs_phase_b'] epochs)
  - Evaluate on holdout using evaluate_holdout()
  - Save results: {'temperature': T, 'holdout_f1': f1, 'holdout_accuracy': acc}
  - Save model checkpoint: PATHS['STUDENT_CHECKPOINT_DIR'] + f'/sweep_T{T}_final.pt'
  - If checkpoint already exists for this T, skip training and just evaluate

Print comparison table:
```
| Temperature | Holdout F1 | vs Baseline | vs Gate |
|-------------|-----------|-------------|---------|
| T=2         | {f1}      | +{delta}    | {pass?} |
| T=3         | {f1}      | +{delta}    | {pass?} |
| T=4         | {f1}      | +{delta}    | {pass?} |
| T=5         | {f1}      | +{delta}    | {pass?} |
```

Select best_T = argmax(holdout_f1). Print: "Optimal temperature: T={best_T} (F1={best_f1:.4f}). Selected by holdout F1 per TEXT-04."

**Cell 13 -- Gate check + final evaluation:**
Load the best model (from temperature sweep winner). Run comprehensive evaluation:
- Overall holdout F1 (binary)
- Per-vector breakdown (matching Phase 3 format)
- Classification report (precision, recall, F1 per class)
- Comparison to all reference points:
  ```
  Phase 2 baseline (MobileBERT direct fine-tune): F1 = 0.7719
  Phase A (soft-labels-only):                     F1 = {phase_a_f1}
  Phase B (full distillation, best T):            F1 = {phase_b_f1}
  Teacher ceiling:                                F1 = 0.8052
  
  Delta vs baseline: {phase_b_f1 - 0.7719:.4f}
  Gate (>= +3 F1 points, F1 >= 0.8019): {'PASSED' if >= 0.8019 else 'FAILED -- see D-06 recovery below'}
  ```

If gate FAILS, print D-06 recovery analysis per CONTEXT.md:
```
D-06 RECOVERY TRIGGERED
========================
Student plateau F1: {phase_b_f1}
Teacher ceiling F1: 0.8052
Gap to gate: {0.8019 - phase_b_f1:.4f}

Error analysis needed:
1. Which holdout samples does student get wrong that teacher gets right?
2. Are failures concentrated in specific vectors (safe/phishing)?
3. Headroom analysis: teacher is only {0.8052 - phase_b_f1:.4f} pts above student

Options:
A) Relax gate to 2-point gain (F1 >= 0.7919): {'PASSED' if >= 0.7919 else 'FAILED'}
B) Improve teacher first (requires Phase 3 re-run)
C) Iterate on distillation hyperparameters (beta, gamma, LR, epochs)

Recommendation: If student is within 1 F1 point of teacher, the distillation has
transferred ~maximum possible knowledge. Proceed to Phase 5 with this checkpoint.
```

**Cell 14 -- Save final checkpoint:**
Save the best model (temperature sweep winner) to:
1. Local: `research/models/student_finetuned/distilled_mobilebert.pt` (model state_dict + config)
2. Google Drive: PATHS['STUDENT_CHECKPOINT_DIR'] + '/final/distilled_mobilebert.pt'
3. Also save metadata JSON: {
     'phase': 4,
     'model': 'google/mobilebert-uncased',
     'teacher': 'microsoft/deberta-v3-large',
     'best_temperature': best_T,
     'holdout_f1': best_f1,
     'baseline_f1': 0.7719,
     'delta_f1': best_f1 - 0.7719,
     'gate_passed': best_f1 >= 0.8019,
     'training_config': ACTIVE_CONFIG,
     'phase_a_f1': phase_a_f1,
     'phase_b_f1': best_f1,
   }

Verify file exists and print size.

**Cell 15 -- T4-to-A100 migration guide (markdown cell, per D-07):**
Markdown cell with step-by-step instructions matching Phase 3 pattern:
1. Runtime > Change runtime type > A100 GPU
2. Re-run Cell 1 (Drive mounting)
3. Change ACTIVE_CONFIG = A100_CONFIG in Cell 2
4. Re-run Cells 3-7 (setup cells)
5. Training cells will auto-resume from Drive checkpoints (D-09)
6. If switching mid-sweep, completed temperatures will be skipped automatically

**Cell 16 -- Summary report (markdown-generating code cell):**
Print final summary:
- Phase 4 complete / incomplete
- All key metrics (Phase A F1, Phase B F1 at each T, best T)
- Gate status
- Checkpoint location for Phase 6
- Next step: Phase 5 (Multi-Label Intent Head) if gate passed
  </action>
  <verify>
    <automated>python3 -c "import json; nb=json.load(open('research/notebooks/knowledge_distillation.ipynb')); cells=[c for c in nb['cells'] if c['cell_type']=='code']; assert len(cells) >= 14, f'Expected >= 14 code cells, got {len(cells)}'; src=''.join([''.join(c['source']) for c in cells]); assert 'train_phase_b' in src, 'Missing Phase B training'; assert 'temperature_sweep' in src or 'for T in' in src, 'Missing temperature sweep'; assert 'D-06' in src or 'RECOVERY' in src, 'Missing D-06 recovery'; assert 'student_finetuned' in src, 'Missing checkpoint save path'; assert '0.8019' in src, 'Missing gate threshold'; print('All Cell 11-16 checks passed')"</automated>
  </verify>
  <acceptance_criteria>
    - Notebook contains >= 14 code cells plus markdown cells
    - Cell 11 contains train_phase_b function that loads Phase A checkpoint and adds intermediate losses
    - Cell 11 contains teacher forward pass wrapped in torch.no_grad() (D-04)
    - Cell 11 logs individual loss components (soft, hard, hidden, attention) in first 10 batches per RESEARCH.md Pitfall 2
    - Cell 11 contains use_attention_loss flag per RESEARCH.md Pitfall 3
    - Cell 12 sweeps T in [2, 3, 4, 5], each starting from Phase A checkpoint
    - Cell 12 selects optimal T by holdout F1 (contains "holdout" and "argmax" or "max" for selection)
    - Cell 13 contains gate check comparing to 0.8019 and D-06 recovery path
    - Cell 14 saves to both research/models/student_finetuned/ and Google Drive with metadata JSON
    - Cell 15 is a markdown cell with T4-to-A100 migration steps
    - Cell 16 prints final summary with all metrics
  </acceptance_criteria>
  <done>Phase B training, temperature sweep, gate check, checkpoint save, and migration guide added to notebook. Complete notebook ready for Colab execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify distillation results in Colab</name>
  <files>research/notebooks/knowledge_distillation.ipynb</files>
  <action>
User runs the complete knowledge distillation notebook in Google Colab. This is a human verification checkpoint -- Claude has completed all notebook code; the user executes it on GPU hardware and reports results.

Steps for user:
1. Upload notebook to Google Colab (or open via Drive)
2. Select GPU runtime (T4 recommended, A100 if available)
3. Run all cells sequentially (Cells 1-16)
4. Report: Phase A holdout F1, Phase B best holdout F1, best temperature, gate passed/failed
5. If gate fails, follow D-06 recovery instructions in Cell 13

Expected runtime: ~6-12 hours total on T4 (spread across sessions, checkpoints auto-resume per D-09).
  </action>
  <verify>User reports gate results: Phase A F1, Phase B best F1, best T, gate status (passed/failed/D-06 recovery)</verify>
  <done>User confirms distillation notebook runs successfully in Colab; gate check results reported (F1 >= 0.8019 passed, or D-06 recovery path selected); student checkpoint exists on Google Drive</done>
</task>

</tasks>

<verification>
1. Complete notebook exists at research/notebooks/knowledge_distillation.ipynb with >= 16 cells (code + markdown)
2. Phase A (soft-labels-only) and Phase B (intermediate layer transfer) are both implemented as separate training functions
3. Temperature sweep evaluates T={2,3,4,5} on holdout, selects by holdout F1
4. Gate check compares to baseline F1=0.7719 and gate F1=0.8019
5. D-06 recovery path implemented for gate failure case
6. Checkpoint save to research/models/student_finetuned/ with metadata JSON
7. Teacher always frozen with torch.no_grad() (D-04)
8. Projection layers (24 x nn.Linear(1024, 512)) trained jointly with student (anti-pattern prevention)
9. Loss component logging in first 10 batches for beta calibration (Pitfall 2)
10. T4-to-A100 migration guide present (D-07)
11. All checkpoints saved to Google Drive (D-09)
</verification>

<success_criteria>
- Complete distillation notebook runs in Colab without errors
- Phase A produces holdout F1 > baseline (0.7719)
- Phase B produces holdout F1 > Phase A F1
- Temperature sweep identifies optimal T from {2,3,4,5}
- Gate check: F1 >= 0.8019 (or D-06 recovery documents why and recommends path forward)
- Student checkpoint saved to research/models/student_finetuned/ and Google Drive
- Checkpoint is ready for Phase 6 QAT consumption
</success_criteria>

<output>
After completion, create `.planning/phases/04-knowledge-distillation/04-02-SUMMARY.md`
</output>
